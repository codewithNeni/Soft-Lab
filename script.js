document.addEventListener("DOMContentLoaded", function () {
    // Create an Intersection Observer
    let observer = new IntersectionObserver(function (entries) {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                // Add the class when the element is in view
                entry.target.classList.add('in-view');
            } else {
                // Optionally, you can remove the class when it's out of view
                // entry.target.classList.remove('in-view');
            }
        });
    });

    // Target the .choose-head element
    let target = document.querySelector('.choose-head');
    if (target) {
        observer.observe(target);
    }
});



document.addEventListener("DOMContentLoaded", function () {
    // Create an Intersection Observer
    let observer = new IntersectionObserver(function (entries) {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                // Add the class when the element is in view
                entry.target.classList.add('in-view');
            } else {
                // Optionally, you can remove the class when it's out of view
                // entry.target.classList.remove('in-view');
            }
        });
    });

    // Target the .choose-head element
    let target = document.querySelector('.program-head');
    if (target) {
        observer.observe(target);
    }
});


document.addEventListener("DOMContentLoaded", function () {
    // Create an Intersection Observer
    let observer = new IntersectionObserver(function (entries) {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                // Add the class when the element is in view
                entry.target.classList.add('in-view');
            } else {
                // Optionally, you can remove the class when it's out of view
                // entry.target.classList.remove('in-view');
            }
        });
    });

    // Target the .choose-head element
    let target = document.querySelector('.trainers-head');
    if (target) {
        observer.observe(target);
    }
});


document.addEventListener("DOMContentLoaded", function () {
    // Create an Intersection Observer
    let observer = new IntersectionObserver(function (entries) {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                // Add the class when the element is in view
                entry.target.classList.add('in-view');
            } else {
                // Optionally, you can remove the class when it's out of view
                // entry.target.classList.remove('in-view');
            }
        });
    });

    // Target the .choose-head element
    let target = document.querySelector('.category-head');
    if (target) {
        observer.observe(target);
    }
});



document.addEventListener("DOMContentLoaded", function () {
    // Create an Intersection Observer
    let observer = new IntersectionObserver(function (entries) {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                // Add the class when the element is in view
                entry.target.classList.add('in-view');
            } else {
                // Optionally, you can remove the class when it's out of view
                // entry.target.classList.remove('in-view');
            }
        });
    });

    // Target the .choose-head element
    let target = document.querySelector('.ciriculum-head');
    if (target) {
        observer.observe(target);
    }
});



document.addEventListener("DOMContentLoaded", function () {
    // Create an Intersection Observer
    let observer = new IntersectionObserver(function (entries) {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                // Add the class when the element is in view
                entry.target.classList.add('in-view');
            } else {
                // Optionally, you can remove the class when it's out of view
                // entry.target.classList.remove('in-view');
            }
        });
    });

    // Target the .choose-head element
    let target = document.querySelector('.unique-head');
    if (target) {
        observer.observe(target);
    }
});




document.addEventListener("DOMContentLoaded", function () {
    // Create an Intersection Observer
    let observer = new IntersectionObserver(function (entries) {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                // Add the class when the element is in view
                entry.target.classList.add('in-view');
            } else {
                // Optionally, you can remove the class when it's out of view
                // entry.target.classList.remove('in-view');
            }
        });
    });

    // Target the .choose-head element
    let target = document.querySelector('.demand-left');
    if (target) {
        observer.observe(target);
    }
});



document.addEventListener("DOMContentLoaded", function () {
    // Create an Intersection Observer
    let observer = new IntersectionObserver(function (entries) {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                // Add the class when the element is in view
                entry.target.classList.add('in-view');
            } else {
                // Optionally, you can remove the class when it's out of view
                // entry.target.classList.remove('in-view');
            }
        });
    });

    // Target the .choose-head element
    let target = document.querySelector('.demand-right');
    if (target) {
        observer.observe(target);
    }
});



document.addEventListener("DOMContentLoaded", function () {
    // Create an Intersection Observer
    let observer = new IntersectionObserver(function (entries) {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                // Add the class when the element is in view
                entry.target.classList.add('in-view');
            } else {
                // Optionally, you can remove the class when it's out of view
                // entry.target.classList.remove('in-view');
            }
        });
    });

    // Target the .choose-head element
    let target = document.querySelector('.enroll-left');
    if (target) {
        observer.observe(target);
    }
});




document.addEventListener("DOMContentLoaded", function () {
    // Create an Intersection Observer
    let observer = new IntersectionObserver(function (entries) {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                // Add the class when the element is in view
                entry.target.classList.add('in-view');
            } else {
                // Optionally, you can remove the class when it's out of view
                // entry.target.classList.remove('in-view');
            }
        });
    });

    // Target the .choose-head element
    let target = document.querySelector('.enroll-right');
    if (target) {
        observer.observe(target);
    }
});


document.addEventListener("DOMContentLoaded", function () {
    // Create an Intersection Observer
    let observer = new IntersectionObserver(function (entries) {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                // Add the class when the element is in view
                entry.target.classList.add('in-view');
            } else {
                // Optionally, you can remove the class when it's out of view
                // entry.target.classList.remove('in-view');
            }
        });
    });

    // Target the .choose-head element
    let target = document.querySelector('.contact-left');
    if (target) {
        observer.observe(target);
    }
});


document.addEventListener("DOMContentLoaded", function () {
    // Create an Intersection Observer
    let observer = new IntersectionObserver(function (entries) {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                // Add the class when the element is in view
                entry.target.classList.add('in-view');
            } else {
                // Optionally, you can remove the class when it's out of view
                // entry.target.classList.remove('in-view');
            }
        });
    });

    // Target the .choose-head element
    let target = document.querySelector('.contact-form');
    if (target) {
        observer.observe(target);
    }
});